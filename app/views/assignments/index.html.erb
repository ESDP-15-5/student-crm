
<div class="digg_pagination" style="margin:auto">
  <%= will_paginate @hw_table %>
</div>

<table class="table table-bordered table-striped">
<tr>
  <th> Курс </th>
  <th> Занятие </th>
  <th> Гр№ </th>
  <th> ФИО </th>
  <th> Домашняя работа </th>
  <th> Дата загрузки </th>
</tr>

<% @hw_table.each do |row| %>
  <tr>
    <td><%= row[:course] %></td>
    <td><%= row[:period] %></td>
    <td><%= row[:group] %></td>
    <td><%= row[:user] %></td>
    <% if row[:assigment].empty? %>
        <td>Не загружено</td>
        <td></td>
    <% else %>
        <td><%= row[:assigment].name %></td>
        <td><%= row[:assigment].created_at.strftime("%H:%M %d %b %Y") %></td>
    <%	end %>

  </tr>
<% end %>

</table>
