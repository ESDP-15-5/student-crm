<div class="row">
  <div class="col-md-12">
    <%= link_to 'Курсы', courses_path %> >
    <%= link_to @course.name, course_path(@course.id) %> >
    <%= @course_element.theme %> >
    <%= 'Обновление материала' %>
  </div>
</div>

<%= provide :button_text, 'Обновить материал' %>
<%= render 'form'  %>