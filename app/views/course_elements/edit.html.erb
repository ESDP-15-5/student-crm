Обновление элемента курса

<%= form_for [@course, @course_element] do |f| %>
    <div class="form-group">
      <%= f.label :course_id, class: 'col-sm-2 control-label' %>
      <div class="col-sm-10">
        <%= f.text_field :course_id, class: 'form-control', value: @course.name, :disabled => true %>
      </div>
    </div>

    <div class="form-group">
      <%= f.label :theme, class: 'col-sm-2 control-label' %>
      <div class="col-sm-10">
        <%= f.text_field :theme, class: 'form-control' %>
      </div>
    </div>
    <div class="form-group">
      <%= f.label :element_type, class: 'col-sm-2 control-label' %>
      <div class="col-sm-10">
        <%= f.select(:element_type,
                     CourseElement::ELEMENT_TYPES,
                     class: 'form-control')
        %>
      </div>
    </div>

    <div class="form-group">
      <div class="col-sm-offset-2 col-sm-10">
        <%= f.submit class: 'btn btn-primary' %>
      </div>
    </div>
<% end %>